# 배포 준비 체크리스트 ✅

## 1️⃣ 로컬 개발 서버 실행 (현재 단계)

개발 서버가 실행 중입니다. 브라우저에서 다음 주소로 접속하세요:

**http://localhost:5173**

### 확인 사항:
- [ ] 사이트가 정상적으로 열리는지 확인
- [ ] 캘린더가 표시되는지 확인
- [ ] 미션 체크 모달이 열리는지 확인
- [ ] Firebase 연결 확인 (브라우저 콘솔에서 "✅ Firebase 초기화 완료" 메시지 확인)

---

## 2️⃣ Git 저장소 초기화 및 GitHub 연결

### 2-1. Git 초기화 (아직 안 했다면)

```bash
# Git 초기화
git init

# .gitignore 확인 (이미 있음)
# node_modules, dist 등은 자동으로 제외됨
```

### 2-2. GitHub 저장소 생성

1. GitHub.com에 로그인
2. **New repository** 클릭
3. 저장소 이름 입력 (예: `marble-mission`)
4. **Public** 또는 **Private** 선택
5. **Create repository** 클릭
6. 생성된 저장소 URL 복사 (예: `https://github.com/사용자명/저장소명.git`)

### 2-3. 로컬과 GitHub 연결

```bash
# 원격 저장소 추가
git remote add origin https://github.com/사용자명/저장소명.git

# 파일 추가
git add .

# 첫 커밋
git commit -m "Initial commit: 청년회 부서대항전 미션 체크 시스템"

# main 브랜치에 푸시
git branch -M main
git push -u origin main
```

---

## 3️⃣ Firebase 토큰 생성 및 GitHub Secrets 설정

### 3-1. Firebase CLI 설치 및 로그인

```bash
# Firebase CLI 설치 (로컬에서 한 번만)
npm install -g firebase-tools

# Firebase 로그인
firebase login
```

브라우저가 열리면 Google 계정으로 로그인하세요.

### 3-2. CI용 토큰 생성

```bash
# CI용 토큰 생성 (출력되는 토큰을 복사하세요!)
firebase login:ci
```

**중요**: 출력되는 긴 토큰을 복사해두세요. 예시:
```
1//abc123def456...
```

### 3-3. GitHub Secrets 설정

1. GitHub 저장소 페이지로 이동
2. **Settings** 탭 클릭
3. 왼쪽 메뉴에서 **Secrets and variables** > **Actions** 클릭
4. **New repository secret** 버튼 클릭
5. 다음 정보 입력:
   - **Name**: `FIREBASE_TOKEN`
   - **Value**: 위에서 복사한 Firebase 토큰
6. **Add secret** 클릭

---

## 4️⃣ 배포 테스트

### 4-1. 코드 푸시

```bash
# 변경사항 커밋
git add .
git commit -m "Add GitHub Actions auto-deploy workflow"

# GitHub에 푸시
git push origin main
```

### 4-2. 배포 확인

1. GitHub 저장소 페이지로 이동
2. **Actions** 탭 클릭
3. "Deploy to Firebase Hosting" 워크플로우가 실행되는지 확인
4. 약 2-3분 후 배포 완료
5. 배포 완료 후 `https://marblemisson.web.app` 접속하여 확인

---

## 5️⃣ 배포 후 확인 사항

- [ ] 배포된 사이트가 정상적으로 열리는지 확인
- [ ] Firebase Firestore에 데이터가 저장되는지 확인
- [ ] 다른 기기에서 접속하여 실시간 동기화가 되는지 확인

---

## 📋 현재 상태

✅ **완료된 항목:**
- Firebase 설정 완료 (`src/firebase/config.js`)
- Firebase Hosting 설정 완료 (`firebase.json`, `.firebaserc`)
- GitHub Actions 워크플로우 생성 (`.github/workflows/deploy.yml`)
- 부서별 명단 업데이트 완료

⏳ **진행 필요:**
- Git 저장소 초기화 및 GitHub 연결
- Firebase 토큰 생성 및 GitHub Secrets 설정
- 첫 배포 실행

---

## 🚨 주의사항

1. **Firebase 토큰 보안**
   - 토큰은 절대 코드에 직접 작성하지 마세요
   - GitHub Secrets에만 저장하세요
   - 토큰이 유출되면 `firebase login:ci`로 새로 생성하세요

2. **.gitignore 확인**
   - `node_modules`, `dist` 등은 자동으로 제외됩니다
   - Firebase 설정 파일(`src/firebase/config.js`)은 포함됩니다 (이미 실제 값이 들어있음)

3. **배포 실패 시**
   - GitHub Actions 탭에서 에러 로그 확인
   - Firebase 토큰이 올바른지 확인
   - `.firebaserc`의 프로젝트 ID 확인

